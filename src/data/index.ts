import { DataContext } from "./context";
import { useData } from "./hook";
import { DataProvider, KapseliData } from "./provider";

export { DataContext, useData, DataProvider };

export type { KapseliData };
